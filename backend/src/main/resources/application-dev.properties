# --- server ---
server.port=8080

# --- H2 in-memory database ---
spring.datasource.url=jdbc:h2:mem:coursereg;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# --- JPA/Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Use H2 dialect for H2 (even in Postgres compatibility mode)
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# --- H2 console (optional) ---
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# --- Swagger/OpenAPI ---
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# --- (Optional) relax security in dev so frontend can call the API without auth ---
# If your project uses Spring Security and you don't have a dev SecurityConfig, temporarily disable auto security:
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
